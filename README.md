# Bash Port Scanner

A simple Bash-based port scanner that checks open TCP ports on a target host.

## Features

- Scans ports 1-1024
- Detects open TCP ports
- Lightweight and easy to use

## Usage

```bash
chmod +x port_scanner.sh
./port_scanner.sh
